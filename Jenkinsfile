pipeline {
  agent any
  stages {
    stage('test') {
      steps {
        sh 'echo test'
      }
    }
    stage('push') {
      steps {
        echo 'push'
      }
    }
    stage('deploy') {
      steps {
        echo 'deploy'
      }
    }
  }
}